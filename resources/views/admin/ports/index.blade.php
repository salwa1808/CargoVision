@extends('layouts.app')
@section('content')
<div class="container-fluid py-3"><div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 fw-bold">Port Dataset Management</h1><p class="text-muted mb-0">Kelola master pelabuhan yang digunakan CargoVision.</p></div><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#portModal">+ Tambah Pelabuhan</button></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="card card-body"><form class="mb-4"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Cari pelabuhan..."></form><div class="table-responsive"><table class="table table-hover"><thead><tr><th>Pelabuhan</th><th>Negara</th><th>Latitude</th><th>Longitude</th><th>Aksi</th></tr></thead><tbody>@forelse($ports as $port)<tr><td class="fw-bold">{{ $port->name }}</td><td>{{ $port->country?->name }}</td><td>{{ $port->latitude }}</td><td>{{ $port->longitude }}</td><td><div class="d-flex gap-2"><button class="btn btn-sm btn-outline-info edit-port" data-port="{{ base64_encode($port->toJson()) }}">Edit</button><form method="POST" action="{{ route('admin.ports.destroy',$port) }}" onsubmit="return confirm('Hapus pelabuhan ini?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Hapus</button></form></div></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">Belum ada data pelabuhan.</td></tr>@endforelse</tbody></table></div>{{ $ports->links() }}</div></div>
@foreach(['create'=>['portModal','Tambah Pelabuhan',route('admin.ports.store')],'edit'=>['editPortModal','Edit Pelabuhan','']] as $kind=>[$id,$title,$action])<div class="modal fade" id="{{ $id }}"><div class="modal-dialog modal-dialog-centered"><form class="modal-content" id="{{ $kind }}PortForm" method="POST" action="{{ $action }}">@csrf @if($kind==='edit')@method('PUT')@endif<div class="modal-header"><h5>{{ $title }}</h5><button class="btn-close btn-close-white" data-bs-dismiss="modal"></button></div><div class="modal-body"><label class="form-label">Negara</label><select class="form-select mb-3" name="country_id" required>@foreach($countries as $country)<option value="{{ $country->id }}">{{ $country->name }}</option>@endforeach</select><label class="form-label">Nama pelabuhan</label><input class="form-control mb-3" name="name" required><div class="row"><div class="col"><label class="form-label">Latitude</label><input class="form-control" type="number" step="any" name="latitude"></div><div class="col"><label class="form-label">Longitude</label><input class="form-control" type="number" step="any" name="longitude"></div></div></div><div class="modal-footer"><button type="button" class="btn btn-outline-light" data-bs-dismiss="modal">Batal</button><button class="btn btn-primary">Simpan</button></div></form></div></div>@endforeach
@endsection
@push('scripts')<script>document.querySelectorAll('.edit-port').forEach(b=>b.addEventListener('click',()=>{const p=JSON.parse(atob(b.dataset.port)),f=document.getElementById('editPortForm');f.action=`{{ url('/admin/ports') }}/${p.id}`;['country_id','name','latitude','longitude'].forEach(k=>f.elements[k].value=p[k]??'');bootstrap.Modal.getOrCreateInstance(document.getElementById('editPortModal')).show()}));</script>@endpush
